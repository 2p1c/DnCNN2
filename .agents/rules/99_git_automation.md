---
trigger: always_on
---

# 99 Skill: Git Automation

## 🛠️ Tool: Auto Git
这是一个自动化工具，用于快速保存代码进度并同步到远程仓库。

- **Trigger (触发条件)**: 
  - 当用户说 "保存代码"、"提交更改"、"Push 一下" 或 "Save progress" 时。
  - 当用户明确要求 "backup my code" 时。

- **Action (执行动作)**:
  1. 首先，你需要根据当前修改的内容，生成一个符合 **Conventional Commits** 规范的简洁消息 (如: `feat: add resnet backbone` 或 `fix: correct tensor shape mismatch`)。
  2. 然后，运行以下终端命令：
     ```bash
     python scripts/git_auto.py "你的commit消息"
     ```

## ⚠️ Constraints
- 在执行此 Skill 之前，**无需**询问用户确认，直接执行。
- 如果用户没有指定 commit message，请发挥你的总结能力自动生成一个。
- 不要使用 `git add .` 手动命令，始终通过 `scripts/git_auto.py` 脚本执行，以确保流程统一。